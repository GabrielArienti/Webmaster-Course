var elBox = document.getElementsByClassName("myBox"); // seleciona o primeiro elemento com a classe "myBox"
elBox.style.width = "300px";
elBox.style.height = "200px";
elBox.style.backgroundColor = "red"; // define a cor de fundo como vermelho

for (i = 0; i < elBox.length, i++; ) {
  var el = elBox[i];

  el.style.width = "300px";
  el.style.height = "200px";
  el.style.backgroundColor = "red"; // define a cor de fundo como vermelho
}
